import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  optionContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  optionIcon: {
    fontSize: 28,
    color: 'orange',
  },
  option: {
    fontSize: 20,
    color: 'black',
    paddingVertical: 12,
    paddingHorizontal: 8,
  },
});
